# npm run dev:mock -- no backend, no cluster, no Pi.
VITE_API_MODE=mock

# Unused in mock mode, but kept defined so nothing reads undefined.
VITE_API_BASE_URL=http://localhost

VITE_RELOCK_MINUTES=5

# Change this to reshuffle the generated events.
VITE_MOCK_SEED=1337
